name: Quantum Tools source build

on:
  push:
    branches:
      - "source/3.0.6-dev.9-placement-editor-v5sync"
      - "source/3.0.6-dev.10-complete-reconstruction"
      - "feature/schematic-create-parity-ui"
      - "feature/full-legacy-reconstruction"
  pull_request:
    branches:
      - "main"
      - "source/3.0.6-dev.10-complete-reconstruction"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'

      - name: Compile Quantum Tools
        run: gradle build --stacktrace

      - name: Upload compiled JAR
        uses: actions/upload-artifact@v4
        with:
          name: quantum-tools-source-build
          path: build/libs/*.jar
          if-no-files-found: error
